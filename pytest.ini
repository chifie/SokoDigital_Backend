[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short --cov=app --cov-report=term-missing:skip-covered --cov-report=xml:coverage.xml
markers =
    integration: Tests that require a running PostgreSQL database
