---
# NOTE: This file is a template. Do NOT commit real secrets to version control.
# Generate actual secrets with:
#
#   kubectl create secret generic sokodigital-secrets \
#     --from-literal=DATABASE_URL='postgresql+asyncpg://user:pass@host:5432/sokodigital_db' \
#     --from-literal=SECRET_KEY='your-jwt-secret' \
#     --from-literal=SMTP_HOST='smtp.sendgrid.net' \
#     --from-literal=SMTP_USER='apikey' \
#     --from-literal=SMTP_PASSWORD='your-smtp-password' \
#     --from-literal=SMTP_FROM_EMAIL='noreply@sokodigital.com' \
#     --from-literal=REDIS_URL='redis://redis-service:6379/0' \
#     --from-literal=AI_API_KEY='your-openai-key' \
#     --dry-run=client -o yaml
#
# Or use SealedSecrets / External Secrets Operator for GitOps workflows.

apiVersion: v1
kind: Secret
metadata:
  name: sokodigital-secrets
  labels:
    app: sokodigital
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@postgres-service:5432/sokodigital_db"
  SECRET_KEY: "change-me-in-production"
  SMTP_HOST: ""
  SMTP_USER: ""
  SMTP_PASSWORD: ""
  SMTP_FROM_EMAIL: "noreply@sokodigital.com"
  REDIS_URL: ""
  AI_API_KEY: ""
